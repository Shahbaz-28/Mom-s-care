# Frontend — Caregiver Dashboard

Next.js app for **Caregiver's Second Set of Eyes**.

## Setup

```bash
npm install
cp .env.example .env.local
# Fill in Supabase keys (+ optional Telegram)
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Scripts

| Command | Description |
|---------|-------------|
| `npm run dev` | Development server |
| `npm run build` | Production build |
| `npm run start` | Run production build |
| `npm run lint` | ESLint |

## Environment

See [`.env.example`](.env.example). Never commit `.env.local`.

Full project docs: [../README.md](../README.md)
